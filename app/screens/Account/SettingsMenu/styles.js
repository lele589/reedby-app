import { StyleSheet } from "react-native";
import { Colors } from "../../../styles";

export const styles = StyleSheet.create({
    mainTitle: {
        paddingTop: 10,
        paddingLeft: 10,
        paddingRight: 20,
        color: Colors.yellow,
        fontWeight: '600',
        fontSize: 22
    },
});