import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    view: {
        display: 'flex',
        alignItems: 'center',
        paddingTop: 20,
    },
    noResultsImage: {
        height: 200,
        marginTop: 20
    },
    text: {
        fontSize: 18
    }
});