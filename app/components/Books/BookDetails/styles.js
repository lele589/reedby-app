import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    scrollView: {
        height: '100%'
    },
    tagView: {
        alignItems: 'center',
        flexDirection: 'row',
        flexWrap: 'wrap',
        marginBottom: 10,
        marginTop: -25,
    },
    tag: {
        alignSelf: 'flex-start',
        width: 'auto',
    }
});